language:
  name: "dutch"
  code: "nl"

text-to-speech:
  espeak:
    voice: "nl"

training:
  acoustic-model-type: "pocketsphinx"
  large-files:
    - !env "${profile_dir}/base_dictionary.txt"
    - !env "${profile_dir}/language_model.txt"
    - !env "${profile_dir}/g2p.fst"
